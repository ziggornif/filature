# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# SQLite runtime data (WAL mode)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-*

# Local env / secrets
.env
*.local.toml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code local settings + transient temp files
.claude/settings.local.json
.claude/*.tmp.*

# local dev runtime config (db url, bind)
filature.toml
e2e/node_modules/
e2e/playwright-report/
e2e/test-results/

# lavish review artifacts (ephemeral design surfaces)
.lavish/
