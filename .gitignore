# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# SQLite runtime data (WAL mode)
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-*

# Local env / secrets
.env
*.local.toml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Claude Code local settings + transient temp files
.claude/settings.local.json
.claude/*.tmp.*

# local dev runtime config (db url, bind)
filature.toml
